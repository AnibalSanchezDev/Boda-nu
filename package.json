{
  "dependencies": {
    "file-saver": "^2.0.5",
    "firebase": "^11.6.0",
    "firebase-functions": "^6.3.2",
    "jszip": "^3.10.1",
    "react-slick": "^0.30.3",
    "slick-carousel": "^1.8.1"
  },
  "scripts": {
    "deploy": "cd frontend && npm install && npm run build && cd .. && firebase deploy"
  }
}
